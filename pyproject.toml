[project]
name = "series-ia"
version = "0.1.0"
description = "Pipeline scripts for AI-assisted series production."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "nltk",
    "orjson",
    "pydantic",
    "python-dotenv",
    "pyyaml",
    "requests>=2.34.2",
    "rich",
    "sentence-transformers",
    "spacy",
    "streamlit>=1.57.0",
    "tqdm",
    "typer",
]

[tool.uv]
package = false
